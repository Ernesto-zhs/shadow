server:
  port: 8080

spring:
  application:
    name: shadow-api-admin
  profiles:
    active: dev

  redis: #redis配置信息
    lettuce:
      pool:
        min-idle:
        max-idle:
        max-wait:
        max-active:
        time-between-eviction-runs:
      shutdown-timeout: 30000ms
